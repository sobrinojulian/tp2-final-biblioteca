{
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file .env server.js",
    "watch": "node --env-file .env --watch-path=. server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "express": "^4.19.2",
    "joi": "^17.13.3"
  },
  "devDependencies": {
    "chai": "^5.1.1",
    "mocha": "^10.5.2",
    "supertest": "^7.0.0"
  }
}
